# El-Matadero
create a folder called El-Matadero, inside that put the fxmanifest.lua and add a stream folder inside the stream folder put everything else 
make sure to ensure it in your server cfg and your redy to go
